# CS1090A Fall 2026 -- Homework 0
#
#   uv venv
#   uv pip install -r requirements.txt
#   source .venv/bin/activate      # Windows: .venv\Scripts\activate
#   jupyter lab
#
# This file is HW0 only -- the four libraries the assignment imports, plus otter
# for the grader.check cells and jupyterlab to run it.
#
# The coding assistant and the wider course stack are optional extras, and live in
# pyproject.toml because requirements.txt has no way to express them:
#
#   uv sync --extra ai-tools     # the in-notebook assistant
#   uv sync --extra full         # the whole course stack, mirroring FASOnDemand
#
# SETUP.md explains which to pick.
numpy>=2.3,<3
scipy>=1.16,<2
matplotlib>=3.10,<4
pandas>=2.3,<3
otter-grader>=7,<8
jupyterlab>=4
jupyter>=1.1
